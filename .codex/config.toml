# bOS — OpenAI Codex Configuration
# Codex reads AGENTS.md by default. This config adds fallbacks and tuning.

# Allow Codex to also read CLAUDE.md (full spec) if needed
project_doc_fallback_filenames = ["CLAUDE.md", "COPILOT.md"]

# bOS CLAUDE.md is ~30k chars — raise limit to accommodate
project_doc_max_bytes = 65536
