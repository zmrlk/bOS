# Optional ntfy (not Lite)

`morning-push.sh`, `email-monitor.sh`, `install-layer3.sh`, `com.bos.morning-push.plist`.

Requires a topic in `.secrets/ntfy.env`. Hard-fail if missing. Not wired in `.claude/settings.json`.

Install: `zsh examples/hooks/ntfy/install-layer3.sh --dry-run` then without `--dry-run` if you want it.
