# Not Lite

`supabase/`, `templates/n8n/`, `skills/vault`, `skills/schedule` are optional samples. Clone → works without them. Secrets: create `.secrets/` yourself (`chmod 700` / files `600`); never paste keys into chat.
